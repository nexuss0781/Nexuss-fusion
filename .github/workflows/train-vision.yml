name: train-vision

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["nexuss_fusion/**", "bench/space/**", ".github/workflows/train-vision.yml"]

jobs:
  # Step 1: Prepare data + split into chunks
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Unit tests
        run: pytest -q

      - name: Extract features for SPACE images
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --extract --extract-only

      - name: Split SPACE images into chunks
        run: |
          python - <<'PYEOF'
          import json, shutil
          from pathlib import Path

          space = Path("bench/space")
          captions_raw = json.loads((space / "captions.json").read_text())
          images = sorted([*space.glob("*.jpg"), *space.glob("*.png")])
          n_chunks = min(3, len(images))
          chunk_size = max(1, len(images) // n_chunks)

          for i in range(n_chunks):
              chunk_dir = Path(f"chunks/chunk_{i+1:02d}/images")
              chunk_dir.mkdir(parents=True, exist_ok=True)
              start = i * chunk_size
              end = start + chunk_size if i < n_chunks - 1 else len(images)
              chunk_caps = {}
              for img in images[start:end]:
                  shutil.copy2(img, chunk_dir / img.name)
                  if img.name in captions_raw:
                      chunk_caps[img.name] = captions_raw[img.name]
              (Path(f"chunks/chunk_{i+1:02d}") / "captions.json").write_text(
                  json.dumps(chunk_caps, indent=2)
              )
              print(f"chunk_{i+1:02d}: {len(list(chunk_dir.glob('*')))} images")
          print(f"created {n_chunks} chunks from {len(images)} images")
          PYEOF

      - name: Upload chunks artifact
        uses: actions/upload-artifact@v4
        with:
          name: all-chunks
          path: chunks/
          retention-days: 1

  # Step 2: Train on each chunk (3 parallel jobs for test)
  train:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 360
    strategy:
      fail-fast: false
      matrix:
        chunk: [1, 2, 3]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Download chunks artifact
        uses: actions/download-artifact@v4
        with:
          name: all-chunks
          path: chunks/

      - name: Extract features for this chunk
        run: |
          CHUNK_NUM=$(printf '%02d' ${{ matrix.chunk }})
          python -m nexuss_fusion.run.phase2 --images-dir "chunks/chunk_${CHUNK_NUM}" --cache features-cache --extract --extract-only

      - name: Train chunk ${{ matrix.chunk }}
        run: |
          CHUNK_NUM=$(printf '%02d' ${{ matrix.chunk }})
          python -m nexuss_fusion.run.train_chunk \
            --chunk-dir "chunks/chunk_${CHUNK_NUM}/images" \
            --captions-file "chunks/chunk_${CHUNK_NUM}/captions.json" \
            --cache features-cache \
            --out "chunk_${CHUNK_NUM}_model" \
            --epochs 200 \
            --checkpoint-every 50 \
            --verbose

      - name: Upload chunk model
        uses: actions/upload-artifact@v4
        with:
          name: chunk-${{ matrix.chunk }}-model
          path: chunk_*_model/
          retention-days: 7

  # Step 3: Merge all chunks into one model
  merge:
    needs: train
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words

      - name: Download all chunk models
        uses: actions/download-artifact@v4
        with:
          pattern: chunk-*-model
          path: chunks/

      - name: Merge into final model
        run: |
          python -m nexuss_fusion.run.merge_chunks \
            --chunks-dir chunks \
            --out final_model

      - name: Surface merge info
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("final_model/merge_info.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload final model
        uses: actions/upload-artifact@v4
        with:
          name: nexuss-fusion-vision-model
          path: final_model/
          retention-days: 30
