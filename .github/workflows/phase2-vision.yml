name: phase2-vision

on:
  push:
    branches: [main]
    paths: ["nexuss_fusion/**", "bench/space/**", ".github/workflows/phase2-vision.yml"]

concurrency:
  group: phase2-vision-${{ github.ref }}
  cancel-in-progress: true

jobs:
  phase2:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Unit tests
        run: pytest -q

      - name: Generate synthetic calibration images + captions
        run: python scripts/make_synthetic_calib.py bench/calib 24 7

      - name: Cache populates (extractors load HF models)
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/calib --cache features-cache --extract --extract-only

      - name: "Phase 2 fit + held-out eval (synthetic sanity: gates recorded, not enforced)"
        run: python -m nexuss_fusion.run.phase2 --images-dir bench/calib --cache features-cache --out phase2-results --no-strict

      - name: Surface results
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("phase2-results/phase2.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2-results
          path: |
            phase2-results/
            features-cache/*.pt

  space-strict:
    needs: phase2
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Cache populates for SPACE benchmark images
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --extract --extract-only

      - name: Phase 2 SPACE strict validation
        run: python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --out phase2-results-space --lam 1e-4

      - name: Surface SPACE results
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("phase2-results-space/phase2.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload SPACE results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: phase2-space-strict
          path: |
            phase2-results-space/
            features-cache/*.pt

  stage1b:
    needs: phase2
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Cache populates for SPACE benchmark images
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --extract --extract-only

      - name: Stage 1b — train vision projector + resampler
        run: |
          python -m nexuss_fusion.run.phase1b --images-dir bench/space --cache features-cache --out phase1b-results

      - name: Surface stage 1b results
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("phase1b-results/phase1b.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload stage 1b results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage1b-results
          path: |
            phase1b-results/
            features-cache/*.pt

  stage2a:
    needs: stage1b
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Cache populates for SPACE benchmark images
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --extract --extract-only

      - name: Stage 1b — train vision projector
        run: |
          python -m nexuss_fusion.run.phase1b --images-dir bench/space --cache features-cache --out phase1b-results

      - name: Stage 2a — train decoder alignment (vision prefix + LM loss)
        run: |
          python -m nexuss_fusion.run.phase2a --images-dir bench/space --cache features-cache --out phase2a-results

      - name: Surface stage 2a results
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("phase2a-results/phase2a.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload stage 2a results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage2a-results
          path: |
            phase2a-results/
            phase1b-results/
            features-cache/*.pt

  stage2b:
    needs: stage2a
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Cache populates for SPACE benchmark images
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --extract --extract-only

      - name: Stage 1b — train vision projector
        run: |
          python -m nexuss_fusion.run.phase1b --images-dir bench/space --cache features-cache --out phase1b-results

      - name: Stage 2b — unfreeze decoder + train end-to-end
        run: |
          python -m nexuss_fusion.run.phase2b --images-dir bench/space --cache features-cache --out phase2b-results

      - name: Surface stage 2b results
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("phase2b-results/phase2b.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload stage 2b results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stage2b-results
          path: |
            phase2b-results/
            phase1b-results/
            features-cache/*.pt

  evaluate:
    needs: stage2b
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.46" "pillow>=10" num2words pytest

      - name: Cache populates for SPACE benchmark images
        run: |
          python -m nexuss_fusion.run.phase2 --images-dir bench/space --cache features-cache --extract --extract-only

      - name: Stage 1b — train vision projector
        run: |
          python -m nexuss_fusion.run.phase1b --images-dir bench/space --cache features-cache --out phase1b-results

      - name: Generation quality evaluation (greedy decode + BLEU/ROUGE)
        run: |
          python -m nexuss_fusion.run.evaluate --images-dir bench/space --cache features-cache --out eval-results

      - name: Surface eval results
        run: |
          python - <<'EOF'
          import json
          from pathlib import Path
          p = Path("eval-results/eval.json")
          r = json.loads(p.read_text()) if p.exists() else {"status": "missing"}
          print(json.dumps(r, indent=2))
          EOF

      - name: Upload eval results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eval-results
          path: |
            eval-results/
            phase1b-results/
            features-cache/*.pt